{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "npm run clean:lerna && npm run clean:git",
    "clean:lerna": "lerna clean -y",
    "clean:git": "git clean -fdx"
  },
  "devDependencies": {
    "lerna": "^3.16.4",
    "npm-run-all": "^4.1.5"
  }
}
